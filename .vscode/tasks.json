{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Clean CMake project",
            "type": "shell",
            "command": "docker exec -i spotify_api_c_devcontainer rm -rf ./build",
        },
        {
            "label": "Configure CMake project for debug",
            "type": "shell",
            "command": "docker exec -i spotify_api_c_devcontainer cmake -S . -B ./build -DCMAKE_BUILD_TYPE=Debug",
            "dependsOn": "Clean CMake project"
        },
        {
            "label": "Build CMake project for debug",
            "type": "shell",
            "command": "docker exec -i spotify_api_c_devcontainer cmake --build ./build",
            "group": "build",
            "dependsOn": "Configure CMake project for debug"
        },
    ]
}